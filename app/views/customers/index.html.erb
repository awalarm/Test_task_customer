<h1>Список клиентов</h1>

<table class="table table-bordered table-striped" style="width: 90%; margin: auto;">
  <thead style="text-align: center">
    <tr>
      <th>#</th>
      <th>Имя</th>
      <th>Телефон</th>
      <th>Описание</th>
      <th>Подробнее</th>
    </tr>
  </thead>
  <tbody>
        <% @customers.each do |customer| %>
        <% if customer.black_list == false %>
    <tr>
      <th scope="row"><%= customer.id %></tth>
      <td><%= customer.name %></td>
      <td><%= customer.phone %></td>
      <td><%= customer.text %></td>
      <td><%= link_to 'Show', customer_path(customer) %> |
        <%= link_to 'Edit', edit_customer_path(customer) %> |
        <%= link_to 'ADD BL', customer_path(customer, :customer => {:black_list => 'true'}), method: :put  %>
      </td>
    </tr>
    <% end %>
    <% end %>
  </tbody>
</table>

<br>

<% @customers.reverse_each do |customer| %>
   Вы добавили  <%= customer.name %> с телефоном
    <%= customer.phone %>  <%= time_ago_in_words customer.created_at %> назад. <br>
<% end %>
